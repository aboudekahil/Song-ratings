<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= song.song_name %></title>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <link rel="icon" type="image/x-icon" href="./icons/favicon.ico" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT"
      crossorigin="anonymous"
    />
    <link href="./styles/styles.css" rel="stylesheet" />
    <link href="./styles/song.css" rel="stylesheet" />
  </head>

  <body>
    <div class="d-flex">
      <div>
        <img src="" />
        <h3><%= song.song_name %> - <%= song.song_artist %></h3>
        <small><%=song.song_length%></small>
        <%if(song.song_in_album){ %>
        <h4><%= song.song_in_album %></h4>
        <%}%>
        <br />
        <time datetime="<%= song.song_releasedate%>"
          ><%=song.song_releasedate %></time
        >
      </div>
      <div>
        <% if (!reviews.length) {%>
        <p>no reviews</p>
        <%}%> <% for (let review of reviews) { %>
        <div>
          <p>
            <a href="/p/<%=review.user_name%>"><%= review.user_name %></a> - <%=
            review.rating_last_updated %>
          </p>
          <small><%= review.rating_stars %> / 5</small>
          <p><%= review.rating_review %></p>
        </div>
        <% } %>
      </div>
      <div>
        <h2>Add a review</h2>
        <form action="/api/addReview" id="reviewForm" method="post">
          <div class="form-group">
            <label for="song"></label>
            <input
              type="hidden"
              name="song"
              id="song"
              value="<%= song.song_id %>"
              class="form-control"
            />
          </div>
          <div class="form-group">
            <label for="star">Rate</label>
            <input
              id="star"
              name="stars"
              type="range"
              min="0"
              max="5"
              class="form-range"
            />
          </div>
          <div class="form-group">
            <label for="review">Review</label>
            <textarea
              id="review"
              form="reviewForm"
              name="review"
              class="form-control"
            ></textarea>
          </div>
          <input class="form-control btn btn-primary" type="submit" />
        </form>
      </div>
    </div>
  </body>
</html>
