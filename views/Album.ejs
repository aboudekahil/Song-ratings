<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/x-icon" href="./icons/favicon.ico" />
    <link href="/styles/styles.css" rel="stylesheet" />
    <title><%= album.album_name %></title>
  </head>

  <body>
    <%-include('header', {user})%>
    <main>
      <div class="d-flex">
        <div>
          <img src="" />
          <p>
            <%=album.album_name%> -
            <a href="/artists/<%=album.artist_stagename%>"
              ><%=album.artist_stagename%></a
            >
          </p>
          <p><%=album.album_releasedate%></p>
          <p><%= parseFloat(avgStars).toFixed(2) %>/5</p>
        </div>
        <div>
          <% if(!songs.length){ %>
          <h2>No songs in this album yet</h2>
          <%}%> <% for( let song of songs ) { %>
          <div>
            <p>
              <a
                href="/artists/<%=album.artist_stagename%>/songs/<%= song.song_name %>"
                ><%= song.song_name %></a
              >-<a href="/artists/<%=album.artist_stagename%>"
                ><%=album.artist_stagename%></a
              >
            </p>
            <p><%= song.song_length %></p>
          </div>
          <% } %>
        </div>
      </div>
    </main>
  </body>
</html>
